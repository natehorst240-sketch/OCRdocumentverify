# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# App data
records.db
records.db-journal
*.db
*.db-journal
uploads/*
output/*
!uploads/.gitkeep
!output/.gitkeep

# Real records / runtime state (Docker bind-mounts and backups) — NEVER commit
.env
data/
backups/
.session_lock.json

# Go (handwriting neural net module)
handwriting/handwriting        # built CLI binary
*.gob                          # trained model files
*-idx*-ubyte*                  # downloaded MNIST/EMNIST datasets

# OS / editor
.DS_Store
.idea/
.vscode/

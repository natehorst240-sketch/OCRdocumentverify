# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# App data
records.db
records.db-journal
*.db
*.db-journal
uploads/*
output/*
!uploads/.gitkeep
!output/.gitkeep

# Real records / runtime state (Docker bind-mounts and backups) — NEVER commit
.env
data/
backups/
.session_lock.json

# Go (handwriting neural net module)
# built CLI binary
handwriting/handwriting
# cross-compiled distributables
handwriting/dist/
# trained / embedded model files
*.gob
# downloaded MNIST/EMNIST datasets
*-idx*-ubyte*
*.idx

# OS / editor
.DS_Store
.idea/
.vscode/
